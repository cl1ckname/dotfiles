[Unit]
Description=Automount removable media
PartOf=graphical-session.target
After=graphical-session.target

[Service]
ExecStart=udiskie --notify --tray
Restart=on-failure

[Install]
WantedBy=graphical-session.target
